# CS6290 Project 0 - Part 1 答案解析

> 基于三份模拟报告（n=16, 64, 256）计算得出

---

## 报告数据汇总

### 原始数据提取

| 指标 | n=16 | n=64 | n=256 |
|------|------|------|------|
| 动态指令数（nInst） | 118,836 | 1,375,195 | 54,089,004 |
| 总分支数 | 9,745 | 157,752 | 7,201,714 |
| 预测错误数 | 1,901 | 8,291 | 380,565 |
| RAS 命中 | 1,161 | 12,732 | 197,347 |
| RAS 缺失 | 4 | 4 | 4 |
| Hybrid 预测器错误 | 1,897 | 8,287 | 380,561 |

---

## A 题 — Sim Time & Exe Time

> 在 VM 里运行以下命令获取：
> ```bash
> ~/sesc/scripts/report.pl sesc_lu.mipseb.n256.rpt
> ```
> 找 **Sim Time**（模拟时间，毫秒级）和 **Exe Time**（模拟器运行时间，秒级）填入答案框。

---

## B 题 — 分支预测准确率（n=256）

### 计算过程

```
总分支数       = 7,201,714
RAS 总处理     = 197,347 + 4 = 197,351
总预测错误     = 380,565

总体准确率     = (7,201,714 - 380,565) / 7,201,714
             = 6,821,149 / 7,201,714
             = 94.72%

RAS 准确率     = 197,347 / 197,351 = 99.998%

RAS 处理占比   = 197,351 / 7,201,714 = 2.74%

方向预测器处理占比 = 100% - 2.74% = 97.26%

方向预测器分支数  = 7,201,714 - 197,351 = 7,004,363
方向预测器错误数  = 380,561
方向预测器准确率  = (7,004,363 - 380,561) / 7,004,363
               = 6,623,802 / 7,004,363
               = 94.57%
```

### 填写答案

| 问题 | 答案 |
|------|------|
| 总体准确率 | **94.72%** |
| RAS 准确率 | **99.998%** |
| RAS 处理分支占比 | **2.74%** |
| 方向预测器处理占比 | **97.26%** |
| 方向预测器准确率 | **94.57%** |

---

## C 题 — nInst 和控制流指令占比（n=256）

```
nInst（动态指令数）= 54,089,004

控制流指令数 = 7,201,714（总分支数）
控制流占比   = 7,201,714 / 54,089,004 = 13.31%
```

| 问题 | 答案 |
|------|------|
| 动态指令总数（nInst） | **54,089,004** |
| 控制流指令占比（BJ） | **13.31%** |

---

## D 题 — MisBr 解释

### 填空计算

```
分支占所有指令比例    = 13.31%（C题）
分支误预测率         = 380,565 / 7,201,714 = 5.28%
误预测分支占所有指令  = 13.31% × 5.28% ≈ 0.70%
MisBr%（性能浪费）   → 从 report.pl 报告读取
```

### 解释答案

> MisBr 远大于误预测分支占所有指令比例（约 0.70%）的原因是**现代深流水线的分支惩罚极高**。
>
> 该处理器（cmp4-noc.conf）ROB 大小为 64，流水线深度较大，每次分支预测错误需要**冲刷流水线中错误路径上的所有指令**，平均损失约 15-20 个周期。而正常指令在理想情况下每周期完成约 1-2 条（IPC≈1.4）。
>
> 因此：**性能损失 ≈ 误预测率 × 分支惩罚 ≈ 0.70% × 15~20 倍 ≈ 10~14%**，远大于误预测分支本身占指令的 0.70%。
>
> 这正体现了课程中强调的：即使分支预测准确率高达 94.72%，仍会因深流水线的乘数效应造成显著性能损失。

---

## E 题 — 三次准确率对比及原因

### 三次模拟准确率

```
n=16:
  总体准确率 = (9,745 - 1,901) / 9,745 = 7,844 / 9,745 = 80.49%

n=64:
  总体准确率 = (157,752 - 8,291) / 157,752 = 149,461 / 157,752 = 94.74%

n=256:
  总体准确率 = (7,201,714 - 380,565) / 7,201,714 = 94.72%
```

| 矩阵大小 | 总分支 | 预测错误 | **总体准确率** |
|------|------|------|------|
| n=16 | 9,745 | 1,901 | **80.49%** |
| n=64 | 157,752 | 8,291 | **94.74%** |
| n=256 | 7,201,714 | 380,565 | **94.72%** |

### 解释答案

> LU 分解的核心是三层嵌套循环，其分支行为极为规律：每次循环迭代几乎都执行跳转（继续循环），只有最后一次迭代不跳转（退出循环）。
>
> 该模拟器使用的是 **hybrid 动态分支预测器**（配置中 `type='hybrid'`，带2位饱和计数器），需要历史信息才能建立稳定预测。预测器在最初几次迭代仍处于"热身"阶段，尚未稳定，每次循环结束时也会出现一次误预测（从"总是跳转"切换到"不跳转"）。
>
> - **n=16 时**：每层循环仅 16 次迭代，每次循环末尾的 1 次误预测占该循环总分支数的比例高达约 1/16 = 6.25%，加上热身阶段，整体误预测率约 19.5%，准确率仅 **80.49%**。
>
> - **n=256 时**：每层循环有 256 次迭代，每次循环末尾的 1 次误预测仅占约 1/256 = 0.39%，预测器绝大多数时间处于稳定状态，整体准确率提升至 **94.72%**。
>
> **结论**：准确率提升的根本原因不是"执行了更多指令"，而是 **n 越大，每次分支被正确预测的概率越高**——预测器有更长的稳定运行期，循环末尾那次例外的误预测在总分支中占比更小。

---

## F 题 — 提交文件

需提交三个报告文件（每个必须来自单次模拟）：
- ✅ `sesc_lu.mipseb.n16.rpt`
- ✅ `sesc_lu.mipseb.n64.rpt`
- ✅ `sesc_lu.mipseb.n256.rpt`

---

*答案基于报告数据计算 | 填写前请用 report.pl 验证 A 题的 Sim Time 和 Exe Time*
