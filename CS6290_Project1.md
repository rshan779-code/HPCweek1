# CS 6290：高性能计算机架构
# Project 1

本项目旨在帮助你理解分支预测及乱序处理器的性能。你将再次使用"CS6290 Project VM"虚拟机，和 Project 0 使用的是同一个。和 Project 0 一样，你需要将答案填写在 Word 文档中带红色底框的答案框里，然后在 Canvas 上提交（提交的文件名应为 PRJ1.docx）。

每个答案框中，你必须先给出实际问题的答案（例如一个数字）。然后可以用方括号补充说明，这些说明不是问题所要求的，但可以帮助助教评分。例如，答案 9.7102 可以写成 9.7102 [因为 9.71+0.0002 = 9.7102]。对于要求"为什么"或"解释"的问题，正确答案应简明扼要地说明原因，并提供你所依据的证据。纯猜测即使完全正确，此类题目最多只能得 50% 的分数。

需要额外上传的文件在文档各部分中有说明。提交文件时**不要压缩**（zip、rar 等均不允许）——每个文件应单独上传，并使用作业中指定的文件名。不遵守文件提交和命名规范将扣除最多 20 分；如有文件缺失，与该文件相关的所有答案将得零分（是的，如果 PRJ1.docx 缺失，直接零分）。此外，如果无法明确判断哪个提交文件对应哪个要求文件，该文件将被视为未提交。提交多个疑似对应同一要求的文件同理。总之，如有任何歧义，评分将按相关文件未提交处理。

大多数数值答案应精确到至少两位小数。加速比应至少精确到 4 位小数，并使用**周期数**而非 IPC 计算（report.pl 输出的 IPC 仅保留两位小数）。精度不足或截断（而非正确四舍五入）均会扣分（例如，3.141592 四舍五入到四位小数应为 3.1416，而非 3.1415）。

根据课程规定，本项目为**个人作业**：不允许与其他同学或任何人合作。

---

## Part 1 [20 分]：分支预测器的配置

硬件配置由配置文件描述。本项目将使用 `cmp4-noc.conf` 配置文件，并对其进行修改，因此建议**提前备份**原始配置文件，以便在需要时还原。

处理器（核心）在文件开头附近的 `cpucore` 参数中定义。该文件指定机器有 4 个编号为 0 到 3 的相同核心（`procsPerNode` 参数为 4），每个核心由 `[issueX]` 节描述。在 `[issueX]` 节中，可以看到核心的许多参数，包括：时钟频率设为 1GHz；这是一个乱序核心（`inorder` 设为 false）；每周期最多取指、发射、退休 2 条指令（`issue` 参数为 2）。核心的分支预测器在 `[BPredIssueX]` 节中描述，从名为 `IL1`（由 `instrSource` 参数指定，在 `[IMemory]` 节描述）的结构取指，从名为 `DL1`（在 `[DMemory]` 节描述）的结构读写数据。本部分将修改分支预测器，因此重点看 `[BPredIssueX]` 节。其中预测器类型为 `"Hybrid"`（混合型），并指定了该预测器的各项参数。

`Hybrid` 预测器实际上是一个**锦标赛预测器（Tournament Predictor）**。你需要查看其源码（位于 `~/sesc/src/libcore/` 目录下的 `BPred.h` 和 `BPred.cpp` 文件），确定配置文件中的各参数控制预测器的哪个方面。

> **提示：** `Hybrid` 预测器在 `BPHybrid` 类中实现，查看其构造函数和 `predict` 方法即可了解大部分内容。

### 填空题

该混合预测器的 **元预测器（meta-predictor）** 是一个有 ________ 个条目的表，每个条目是一个 ________-bit 计数器。元预测器根据 PC 地址（即取指令的地址）决定：使用简单（无历史）的计数器数组进行预测，还是使用（局部/全局？）________ 历史预测器进行预测。

- **简单预测器（非历史）**：使用 ________-bit 计数器，共有 ________ 个（该数量在 `BPredIssueX` 节中由参数标签 ________ 指定）。
- **历史预测器**：使用 ________ 位历史，与 PC 地址组合后索引一个有 ________ 个条目的数组（该条目数在配置文件中由参数标签 ________ 指定），每个条目是一个 ________-bit 计数器。

---

## Part 2 [30 分]：更改分支预测器

现在我们将比较几种分支预测器。Project 0 中使用的 LU benchmark 对分支预测器压力不大，因此本次改用 **raytrace benchmark**：

```bash
cd ~/sesc/apps/Splash2/raytrace
make
```

### Part A：三种预测器对比

依次运行以下模拟（以下命令应为单行，`-ort.out` 前有一个空格。注意命令中的连字符应为减号 `-`，复制粘贴可能导致字符替换，请仔细检查）：

**① Hybrid 预测器（默认配置）**
```bash
~/sesc/sesc.opt -f HyA -c ~/sesc/confs/cmp4-noc.conf -ort.out -ert.err raytrace.mipseb -m128 Input/reduced.env
```

**② Oracle（完美方向预测器）**

如未备份，先备份配置文件，然后将预测器 `type` 从 `"Hybrid"` 改为 `"Oracle"`，重新运行：
```bash
~/sesc/sesc.opt -f OrA -c ~/sesc/confs/cmp4-noc.conf -ort.out -ert.err raytrace.mipseb -m128 Input/reduced.env
```
> 注意：此时整体分支预测准确率并非 100%——只有**方向预测器**是完美的，**目标地址预测器**仍是普通 BTB。

**③ NotTaken 预测器（总预测不跳转）**
```bash
~/sesc/sesc.opt -f NTA -c ~/sesc/confs/cmp4-noc.conf -ort.out -ert.err raytrace.mipseb -m128 Input/reduced.env
```

在 Canvas 中提交三个仿真报告文件：`sesc_raytrace.mipseb.HyA`、`sesc_raytrace.mipseb.OrA`、`sesc_raytrace.mipseb.NTA`。

**填写下表**（整体准确率取 report.pl 输出中 BPred 下的数值，加速比相对于 Hybrid）：

| 预测器 | BPred 准确率 | 周期数 | 相对 Hybrid 加速比 |
|--------|------------|--------|-----------------|
| NotTaken | ________ % | ________ C | ________ X |
| Hybrid | ________ % | ________ C | ________ X |
| Oracle | ________ % | ________ C | ________ X |

### Part C：加深流水线后重复实验

将配置文件中 `[issueX]` 节的 `renameDelay` 参数从 **1 改为 7**（流水线加深 6 级），重复上述三次仿真，提交报告文件：`sesc_raytrace.mipseb.HyC`、`sesc_raytrace.mipseb.OrC`、`sesc_raytrace.mipseb.NTC`。

**填写下表**（计算将 renameDelay 从 7 改回 1 的加速比，假设时钟周期时间不变）：

| 预测器 | renameDelay=1 周期数 | renameDelay=7 周期数 | 缩短流水线加速比 |
|--------|-------------------|-------------------|--------------|
| NotTaken | ________ C | ________ C | ________ X |
| Hybrid | ________ C | ________ C | ________ X |
| Oracle | ________ C | ________ C | ________ X |

### Part D：分析

Part C 的结果说明，当处理器流水线加深时，更好的分支预测变得（填"更"或"更不"）________ 重要。

**请解释为什么分支预测的重要性会随流水线深度这样变化：**

> 答：

根据以上仿真结果，至少有两种方法可以估算**默认流水线深度下分支预测错误的惩罚周期数（misprediction penalty）**（即 Part A 中的结果）。

你的最佳估计值为 ________ 周期，请解释你是如何得出的：

> 答：

---

## Part 3 [50 分]：哪些分支容易被预测错？

本部分**恢复使用默认的 `cmp4-noc` 配置**（将配置文件还原为 Part 2 修改之前的内容），继续使用与 Part 2 相同参数的 raytrace benchmark。

### 目标

对程序中每条**静态分支/跳转指令**，分别统计方向预测器（Hybrid 或 NotTaken）**预测正确**和**预测错误**的次数。每条静态指令的完成次数 = 正确次数 + 错误次数。

你需要**修改模拟器源码**，在仿真结束时输出所需数据（按题目要求的顺序输出），且**不得改变仿真报告的内容**。分别对 Hybrid 和 NotTaken 预测器各运行一次仿真。

### 填空题

在两次仿真中，**完成次数在 1–19 次**的静态分支/跳转指令数为 ________，**20–199 次**的为 ________，**200–1999 次**的为 ________，**2000+ 次**的为 ________。

**各频率组的方向预测准确率：**

| 完成次数区间 | Hybrid 准确率 | NT 准确率 |
|-----------|-------------|---------|
| 1–19 | ________ % | ________ % |
| 20–199 | ________ % | ________ % |
| 200–1999 | ________ % | ________ % |
| 2000+ | ________ % | ________ % |

### 分析题

如果使用更大规模的输入运行 raytrace benchmark，Hybrid 预测器和 NT 预测器的整体准确率会如何变化？请简要说明原因：

> 答：

### 提交文件

- 修改后的 `BPred.h` 和 `BPred.cpp`
- 如修改了其他源码，打包为 `OtherCode.zip` 提交
- 两次仿真的输出文件：`rt.out.Hybrid` 和 `rt.out.NT`
- 无需提交仿真报告文件（与 Part A 相同）

---

## 提交文件汇总

| 文件名 | 来源 |
|--------|------|
| `PRJ1.docx` | 本文档（填写答案后） |
| `sesc_raytrace.mipseb.HyA` | Part 2A - Hybrid |
| `sesc_raytrace.mipseb.OrA` | Part 2A - Oracle |
| `sesc_raytrace.mipseb.NTA` | Part 2A - NotTaken |
| `sesc_raytrace.mipseb.HyC` | Part 2C - Hybrid（深流水线） |
| `sesc_raytrace.mipseb.OrC` | Part 2C - Oracle（深流水线） |
| `sesc_raytrace.mipseb.NTC` | Part 2C - NotTaken（深流水线） |
| `BPred.h` | Part 3 修改后的源码 |
| `BPred.cpp` | Part 3 修改后的源码 |
| `rt.out.Hybrid` | Part 3 仿真输出 |
| `rt.out.NT` | Part 3 仿真输出 |
| `OtherCode.zip` | Part 3（如有其他修改文件） |
