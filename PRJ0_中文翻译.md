# CS 6290：高性能计算机体系结构 — Project 0（中文翻译）

> 本文为课程作业文档的中文对照翻译，供理解题意使用。填写答案请在原始的 `PRJ0.docx` 里操作。

---

## 前言与提交规则

本项目的目的是帮助你搭建并使用后续三个 project 所需的模拟器，同时让你更好地理解分支预测的重要性。完成本项目需要安装 VirtualBox 和 CS6290 项目虚拟机（VM）。

**答案格式要求**

- 答案填在本 Word 文档的红色框里，然后提交到 Canvas，提交的文件名必须仍然是 `PRJ0.docx`。
- 不得修改文档中的其他文字。答案必须在红框内，用红色字体加红色下划线。
- 红框的字体设置已经预设好，只要直接填入、不改字体颜色和下划线设置就没问题。
- Georgia Tech 学生有完整的 Office 365 权限，打开和编辑这个文档不应有困难，**其他格式的答案一律不接受**。
- 如果你用别的工具编辑保存，请确保至少保留字体颜色和下划线设置，否则批改者不容易找到你的答案，因此丢的分不会补回。

**答案内容要求**

- 每个答题框里，**必须首先给出问题真正问的答案**（例如一个数字）。之后可以用方括号补充问题没有要求、但你认为有助于批改的解释。
  例如答案 9.7102 可以写成：`9.7102 [因为 9.71+0.0002 等于 9.7102]`
- 对于问「为什么」和/或要求「解释」的题目，正确答案要简洁地说明所描述现象的**成因**，并说明你有什么**证据**支持这个结论。
- **即使猜测完全正确，这类题目也只能拿到 50% 的分数。**

**文件提交要求**

- 需要额外上传的文件在各部分中分别说明。
- **不要打包**（zip、rar 或其他任何格式），每个文件必须单独上传，文件名按作业中指定的写。
- 不遵守文件提交和命名规范最多扣 10 分；如果有文件缺失，与该文件相关的所有答案的分数全部作废（也就是说，**缺少 `PRJ0.docx` 直接零分**）。
- 如果无法非常明确地判断哪个提交文件对应哪个要求的文件，将按缺少该文件处理。
- 提交多个看起来对应同一要求的文件（例如几个同名文件）同样按缺失处理。
- 一句话：只要提交文件的对应关系存在任何歧义，批改时就当作没有提交。

**学术诚信**

按课程规则，**这是个人项目：不允许与其他学生或任何人协作。**

---

## Part 1 [60 分]：模拟器搭建与仿真

### 环境准备

安装 Oracle VirtualBox VMM，然后用 Canvas 上 Project 0 页面的链接下载 CS6290 项目虚拟机。**不要从其他地方获取该虚拟机**（例如 Udacity 网站），那些很可能是过时的旧版本。把 VM 导入 VirtualBox。

从 VirtualBox 里启动 CS6290 项目 VM 后，你会看到一个 Linux 桌面。点击左侧竖排最上方的图标，在「Type Your Command」输入框里输入 "Terminal"，就能得到一个终端窗口。

### ⚠️ 备份警告

在开始产出仿真结果、编辑 VM 内的文件之前，**务必定期把 VM 里的文件备份到别处**（例如宿主机），以免 VM 出问题时丢失工作成果。这种情况偶有发生：过去几个学期有些学生的 VM 无法重启，导致工作丢失。被提到的具体报错是 **"unreliable DMA position"**，唯一可靠的解决办法是使用一份全新的 VM 副本（这会丢失 VM 内创建/修改的所有文件）。

因此，**如果你准备重启 VM（而不只是保存 VM 状态），尤其要注意先做备份。**

### 编译模拟器

模拟器在家目录下的 `sesc` 目录里，首先需要编译它：

```bash
cd ~/sesc
make
```

在 `~/sesc` 目录里应该能看到（用 `ls`）指向可执行文件的 `sesc.opt` 和 `sesc.debug` 两个链接。现在我们只编译了 `sesc.opt`，所以只有那个链接可用，但这已经够了，因为本项目不需要修改模拟器。

### 编译测试程序

模拟器模拟的是使用 MIPS 指令集的处理器，所以需要一个为 MIPS 架构的 Linux 机器编译的应用。这类程序不好找，因此课程已经在 `apps` 子目录里配好了 Splash2 benchmark 套件。这里我们关注 `lu` 这个应用：

```bash
cd apps/Splash2/lu
make
```

现在应该能看到 `lu.mipseb` 可执行文件。这是一个面向**大端序 MIPS 处理器**的可执行文件，正好适合我们的模拟器。

### 运行仿真

模拟器可以模拟各种处理器——你可以改它的分支预测器、每周期执行的指令数等等。所以每次仿真都要告诉它具体模拟什么样的系统，这通过**配置文件**指定。SESC 自带多种配置文件（在 `~/sesc/confs/` 目录），我们使用 `cmp4-noc.conf`。命令行如下（**这是一整行**）：

```bash
~/sesc/sesc.opt -fn256.rpt -c ~/sesc/confs/cmp4-noc.conf -olu.out -elu.err lu.mipseb -n256 -p1
```

> **注意**：上面命令行里的横线是「减号」。从文档粘贴时可能被替换成更长的破折号，那样的话要手动改回减号。另外这是一整行命令，只是在文档里放不下才折行显示。从文档复制粘贴到 VM 时务必小心。

**各参数含义：**

- `-f`：告诉模拟器创建一个以「点 + 指定字符串」结尾的报告文件。报告文件名规则是：`sesc_` + 被模拟应用的可执行文件名（这里是 `lu.mipseb`）+ 点 + 你指定的字符串。所以本例中结果保存在 `sesc_lu.mipseb.n256.rpt`。

  > ⚠️ **非常重要**：在运行会写入同名文件的仿真之前，必须先删除已有的报告文件。如果同名报告文件已存在，模拟器会**追加**写入，导致报告脚本把多次仿真的结果合并统计，你就拿不到本次仿真真正的结果！

- `-c`：指定使用哪个配置文件。配置文件规定了被模拟处理器的各种参数，包括分支预测器的结构、缓存大小等等。
- `-o` 和 `-e`：把应用的标准输出和错误输出分别保存到 `lu.out` 和 `lu.err`。这是必要的，因为模拟器自己也会打印东西，而我们需要单独查看应用的输出和可能的错误，以判断它是否正常执行。
- **格式细节**：`-c` 和配置文件名之间**要有空格**，但 `-o` 和它的文件名之间**不能有空格**，`-e` 同理。
- `lu.mipseb`：要运行的 MIPS 可执行文件。
- `-n256`：告诉 LU 应用（做矩阵的 LU 分解）处理一个 256×256 的矩阵。
- `-p1`：告诉应用只用一个核——**在 Project 3 之前我们都只用一个核！**

### ⚠️ 必须检查运行结果

运行后要**查看应用的输出**：`lu.err` 应该是空的，`lu.out` 结尾应该有 `Total time without initialization: 0` 这一行。

**如果不检查，你可能在毫不知情的情况下得到错误的仿真结果**，例如命令行有拼写错误导致应用中途退出、实际什么都没做。

「应用没有真正跑完」是 benchmarking 中非常常见的陷阱——我们跑这些应用是为了看硬件表现，所以往往不看输出。但如果某次运行因为问题（参数拼错、内存耗尽等）中途中止，由于执行时间大幅缩短，硬件看起来会「特别快」！所以每次运行都必须确认它真的完成了。

### 生成报告摘要

运行完成后会得到一个仿真报告文件。这个文件包含使用的命令行、硬件配置的副本，以及关于本次被模拟执行的大量信息。你可以自己读报告文件，但由于被模拟的处理器非常复杂，里面有大量各部件的统计数据。要提取最相关的统计，可以用 SESC 自带的报告脚本：

```bash
~/sesc/scripts/report.pl sesc_lu.mipseb.n256.rpt
```

可以指定多个文件（或用通配符），脚本会分别打印每个文件的摘要。

### 报告字段说明（对应 A 题）

报告脚本的输出会给出使用的命令行，然后告诉我们：

- **Exe Speed**：仿真进行的速度
- **Exe Time**：模拟器运行期间流逝的真实时间
- **Sim Time**：在被模拟机器上运行该 benchmark 所花的**模拟时间**

注意：我们模拟的这次执行远不到一毫秒，但模拟器完成这次仿真可能花了许多毫秒。

由于仿真在一台机器上执行、却试图模拟另一台机器，报告结果时容易混淆指的是模拟器自身的执行还是被模拟应用的执行。为避免混淆，仿真本身称为 **native（原生）执行**，被模拟的执行有时称为 **target（目标）执行**。

> **【A 题】** 在本次仿真中，被模拟的处理器运行该应用需要 ______ 毫秒的模拟时间（报告中的 "Sim Time"），而模拟器自身完成这次仿真花费了 ______ 秒的原生执行时间（报告中的 "Exe Time"）。

### 分支预测器字段说明（对应 B 题）

报告脚本还会告诉我们被模拟处理器的分支预测器总体准确率（**Total** 下面的数字）。接下来括号里的两个数字在 **RAS** 标签下，报告的是返回地址栈（RAS）的准确率，以及实际使用了 RAS 的分支所占的百分比。

再往后，**BPRed** 标签下是**没有使用 RAS 的分支**的准确率（即它们使用了方向预测器，可能还用了 BTB 和/或 BTAC）。之后是 BTB 和 BTAC 的统计，目前不太重要。

> **【B 题】** 本次仿真中被模拟分支预测器的总体准确率是 ______ 百分比。RAS 往往有很高的准确率，但它只处理了全部动态分支指令（即运行时实际出现的分支）中的 ______ 百分比。这意味着方向预测器处理剩下的 ______ 百分比的动态分支指令，并达到 ______ 百分比的准确率。

### 指令构成字段说明（对应 C 题）

接下来报告告诉我们完成了多少条动态指令（**nInst**），以及这些指令的构成，即控制流指令（分支、跳转、调用、返回等，**BJ** 标签下的数字）、load、store、整数 ALU、浮点 ALU 操作各占 nInst 的百分比。该行剩余部分（LD Forward 及之后）目前不太重要。

> **【C 题】** 本次 lu 在被模拟处理器上运行完成的动态指令数是 ______，控制流指令占这些指令的 ______ 百分比。

### 周期去向字段说明（对应 D 题）

接下来报告告诉我们该应用上处理器达到的总体 **IPC**、处理器为执行指令花费了多少周期，以及这些周期都花在了什么上面。最后这部分细分为：

| 字段 | 含义 |
|---|---|
| **Busy** | 发射有用指令 |
| **LDQ** | 等待 load 队列表项释放 |
| **STQ** | 等待 store 队列表项释放 |
| **IWin** | 等待保留站 |
| **ROB** | 等待 ROB 表项 |
| **Regs** | 等待物理寄存器（模拟器使用类似 Pentium 4 的独立物理寄存器堆做寄存器重命名） |
| **TLB** | 浪费在 TLB 缺失上 |
| **MisBr** | 浪费在因分支误预测而发射错误路径指令上 |
| **ports / maxBr / Br4Clk / Other** | 其他较为晦涩的原因 |

注意：这些「周期去向」项虽然没有百分号，但**都是百分比**。

还要注意模拟器建模的是**多发射处理器**，所以周期去向的拆分实际上会归属**部分周期**。例如在一个四发射处理器中，某个周期里发射了一条有用指令，然后因为 load 队列满了无法再发射，那么这个周期里 0.25 个周期计入 "Busy"，0.75 个周期计入 "LDQ"。

> **【D 题】** 已知分支占全部动态完成指令的 ______ 百分比，且完成的分支指令中只有 ______ 百分比被误预测，我们可以得出：全部动态完成指令中只有 ______ 百分比是误预测的分支。但我们现在看到，分支误预测导致浪费了处理器 ______ 百分比的性能潜力。
>
> **为什么最后这个数字（性能浪费百分比）相比前一个数字（误预测分支占指令的百分比）如此之高？**

### 另外两次仿真（对应 E 题）

现在再跑两次仿真，矩阵规模分别用 16 和 64（注意 `-f` 和 `-n` 参数的变化）：

```bash
~/sesc/sesc.opt -fn16.rpt -c ~/sesc/confs/cmp4-noc.conf -olu.out -elu.err lu.mipseb -n16 -p1

~/sesc/sesc.opt -fn64.rpt -c ~/sesc/confs/cmp4-noc.conf -olu.out -elu.err lu.mipseb -n64 -p1
```

> **注意**：记得每次仿真后都检查 `lu.err` 和 `lu.out`，确认被模拟的运行正确完成！

> **【E 题】** 当我们把矩阵规模（`-n` 参数）从 16 增大到 64、再到 256 时，总体分支预测器准确率从 ______ 提升到 ______，再到 ______ 百分比。
>
> **为什么在 lu 中增大 `-n` 参数会让这个准确率提升？**
>
> **提示 1**：预测器准确率本质上是「一次分支指令的出现被正确预测的总体概率」。也就是说，「我们完成了更多指令」或「我们完成了更多分支」这件事本身，并不能解释为什么现在每一次分支出现都更可能被正确预测。
>
> **提示 2**：我们不要猜测——这道题的答案应当包含**确凿的证据**或**极好的推理**。

> **【F 题】** 提交全部三个模拟器生成的报告文件：`sesc_lu.mipseb.n16.rpt`、`sesc_lu.mipseb.n64.rpt`、`sesc_lu.mipseb.n256.rpt`。记住每个文件都必须是**仅一次仿真**的结果！

---

## Part 2 [40 分]：编译并仿真一个小程序

### 修改 hello.c

现在我们来看看如何编译自己的代码以便在模拟器中运行。为此，修改 `~/sesc/tests/` 目录下的 `hello.c`，让它打印带你名字和姓氏的问候语。

`hello.c` 必须**完全**如下所示（例如不要漏掉 printf 字符串末尾的 `\n`，不要在字符串里加多余空格或其他字符），只有名字部分（粗体斜体处）换成你在 Canvas 上显示的名字和姓氏：

```c
#include <stdio.h>
int main(int argc, char *argv[]){
  printf("Hi! I am Milos Prvulovic\n");
  return 0;
}
```

### 先本地验证

```bash
gcc -o hello hello.c
./hello
```

应该打印出 "Hi! I am Milos Prvulovic" 消息加一个换行（当然是你自己的名字）。

### 交叉编译

代码本地跑通后，就可以尝试为模拟器编译。记住，因为模拟器使用 MIPS 指令集，我们不能用本地编译器，而要用**交叉编译器**——它运行在一台机器上（原生机器），但生成另一台机器（目标机器，即 MIPS）的代码。

安装和配置交叉编译器既耗时又棘手，所以课程已经装好了一个可以直接用。它在 `/mipsroot/cross-tools/bin`，名字是 `mips-unknown-linux-gnu-gcc`。实际上那里还有许多其他交叉编译工具，都以 `mips-unknown-linux-gnu-` 为前缀，后面接常见的 GNU/GCC 工具名。例如 gcc（C 编译器）、g++（C++ 编译器）、gas（汇编器）、objdump（显示目标文件信息的工具）、addr2line（给定指令地址返回源文件和行号）等。

编译命令（**一整行**）：

```bash
/mipsroot/cross-tools/bin/mips-unknown-linux-gnu-gcc -O0 -g -static -mabi=32 -fno-delayed-branch -fno-optimize-sibling-calls -msplit-addresses -march=mips4 -o hello.mipseb hello.c
```

**参数含义：**

| 参数 | 作用 |
|---|---|
| `-O0` | 关闭优化 |
| `-g` | 在可执行文件中包含调试信息 |
| `-static` | 与静态库链接，避免动态链接（那需要在模拟器中额外配置） |
| `-fno-delayed-branch` | 让编译器不使用分支延迟槽 |
| `-fno-optimize-sibling-calls` | 所有过程调用都使用常规的 call/return |
| `-msplit-addresses` | 使用一些能加快寻址的优化 |
| `-mabi=32 -march=mips4` | 使用 MIPS IV 指令集的 32 位实现 |

### 运行仿真

编译器生成 MIPS 可执行文件 `hello.mipseb` 后，就可以在模拟器中执行。**必须完全按下面所示运行**——不要改动命令行选项、不要从其他目录运行、不要给 `hello.mipseb` 指定路径，那会改变被模拟的执行过程，使我们无法核对你是否做对、一切是否正常。

```bash
~/sesc/sesc.opt -fhello0.rpt -c ~/sesc/confs/cmp4-noc.conf -ohello.out hello.mipseb
```

用 `report.pl` 获取仿真报告摘要，填写下列空白：

> **【G 题】** 本次仿真中，被模拟处理器执行了 ______ 条指令（报告中的 "nInst"）。

> **【H 题】** **为什么执行这么小的一个程序也需要如此多的指令？**
>
> **注意**：同样，基于猜测的答案不得分！
>
> **提示 1**：可以用 `mips-unknown-linux-gnu-objdump` 加 `-d` 选项查看 `hello.mipseb` 的汇编清单。在反汇编输出中，搜索 `<main>` 可以找到 C 语言 `main` 的代码。
>
> **提示 2**：可执行文件被加载后，**并不是从 `main()` 开始执行的**。想了解实际发生了什么，可以查阅 GNU 标准 C 库（GLIBC）中 `__glibc_start_main` 函数的文档。
>
> **提示 3**：提示 1 的清单里看到的代码并非全部会被执行。例如可以对 `hello.mipseb` 使用 `mips-unknown-linux-gnu-strip`，能在不破坏程序的前提下大幅减小代码体积（"strip" 工具的作用很容易在网上查到）。即使在 strip 不会移除的代码中，也有大量代码只在特殊情况下（内存分配失败等）才执行，在我们模拟的这次具体运行中根本不会到达。

### 加感叹号实验（对应 I 题）

> **【I 题】** 编辑 `hello.c`，在打印的字符串中加一个感叹号（"!"）：
>
> ```c
> printf("Hi! I am Milos Prvulovic!\n");
> ```
>
> 然后重新交叉编译并仿真（用 `-fhello1` 把报告保存到不同文件）。现在被模拟处理器完成了 ______ 条指令。
>
> 如果仿真指令数**增加**了，就再加一个感叹号，重新编译、重新仿真，`-f` 选项的数值递增，使报告保存到新文件、且文件名中的数字与所加感叹号的个数一致。
>
> **一直这样做，直到完成的指令数相比上一次运行下降为止。** 例如：加一个感叹号时指令数增加，加两个时又增加，但加三个时得到的完成指令数比两个时少，那你就应该在三个感叹号的那次仿真后停止。
>
> **解释为什么最后这次仿真的执行（被模拟）指令数相比前一次下降了？**
>
> **注意**：不要猜测，不要含糊的答案！答案必须非常具体，也就是说，你需要指出**特定代码中的特定行为**。
>
> **提示**：可以用 objdump 看 `main` 实际在做什么（**它并没有调用 printf！**），然后到网上找那个函数的源代码，而不要试图靠看（几乎无法阅读的）反汇编来搞明白它做了什么。

> **【J 题】** 提交：
>
> 1. 你最后一版的 **`hello.c`** 源代码（就是那个让执行指令数比少一个感叹号时更少的版本）
> 2. 原始报告文件 **`sesc_hello.mipseb.hello0.rpt`**
> 3. 最后一次仿真的报告文件（即导致完成指令数下降的那次，例如**可能**是 `sesc_hello.mipseb.hello3.rpt`）

---

## 提交清单速查

| 文件 | 来源 | 说明 |
|---|---|---|
| `PRJ0.docx` | 你填写的答案文档 | **缺失直接零分** |
| `sesc_lu.mipseb.n16.rpt` | Part 1 F | 单次仿真结果 |
| `sesc_lu.mipseb.n64.rpt` | Part 1 F | 单次仿真结果 |
| `sesc_lu.mipseb.n256.rpt` | Part 1 F | 单次仿真结果 |
| `hello.c` | Part 2 J | 最后一版（指令数下降的那版） |
| `sesc_hello.mipseb.hello0.rpt` | Part 2 J | 原始版本 |
| `sesc_hello.mipseb.helloN.rpt` | Part 2 J | 指令数下降的那次 |

**注意事项**：不打包、单独上传、文件名一字不差、每个 `.rpt` 必须来自单次仿真。
