# CS6290 Week 2 - 分支预测 + 谓词执行 + ROB + 指令调度

---

## 1. 分支预测（Branches）

### 核心概念
- 分支指令在流水线中需要多个周期才知道跳转目标/条件
- 错误预测代价：冲刷（Flush）已经取入流水线的错误指令

### 分支惩罚
- 5 级流水线中，分支在 EX 阶段确定，预测错误需冲刷 2 条指令（约 2 周期惩罚）
- 现代深流水线（15-20 级）：错误惩罚高达 15-20 周期

### 分支预测策略
| 策略 | 描述 | 精度 |
|------|------|------|
| **静态：总不跳转** | 预测分支不发生 | ~50% |
| **静态：总跳转** | 预测分支总发生 | ~65% |
| **静态：BTFN** | 向后跳转预测跳，向前预测不跳 | ~65% |
| **动态：1位预测器** | 记录上次结果 | ~80%+ |
| **动态：2位饱和计数器** | 4 状态，需连续2次错才改变预测 | ~85%+ |
| **动态：相关预测器** | 考虑历史分支结果 | ~90%+ |
| **动态：锦标赛预测器** | 组合多个预测器选最准的 | ~95%+ |

### 2 位饱和计数器
```
强不跳(SN) ←→ 弱不跳(WN) ←→ 弱跳(WT) ←→ 强跳(ST)
```
- 比 1 位预测器更稳定（不因一次例外就改变预测）

### BTB（Branch Target Buffer）
- 缓存分支目标地址，避免每次重新计算
- 返回地址用**Return Address Stack** 预测

---

## 2. 谓词执行（Predication）

### 核心概念
- 用 if-conversion 替代条件分支，消除难以预测的分支

### 原理
- 将 if-else 两个分支都执行，用条件标志（predicate）决定哪边结果有效
- 最多浪费 50% 工作量，但完全避免分支预测错误的代价

### 适用场景
| 场景 | 最优方案 |
|------|------|
| 循环分支（强规律性） | 分支预测 |
| 短小 if-else（难预测） | Predication |
| 长 if-else | 分支预测（两边都做代价太高） |

### 实现方式
- ARM 条件执行指令（`ADDEQ`、`MOVNE` 等）
- Intel Itanium predicate 寄存器
- x86 `CMOV`（条件移动）指令

---

## 3. 重排序缓冲（ReOrder Buffer / ROB）

### 核心概念
ROB 解决乱序执行中的两个核心问题：
1. **精确异常（Precise Exceptions）**：异常发生时，只有异常前的指令修改了状态
2. **分支预测错误恢复**：快速撤销错误路径上的指令

### Tomasulo 算法的问题
- 指令乱序 commit，导致异常时无法精确恢复
- 分支预测错误后无法撤销已 commit 的错误指令

### ROB 工作原理
- 所有指令按程序顺序进入 ROB
- 指令可以乱序执行，但**必须按程序顺序 commit（提交）**
- 只有从 ROB 头部 commit 的指令才真正修改架构状态

### ROB 结构
```
[指令 | 目标寄存器 | 结果值 | 完成标志]
  ↑ Head（最老，等待commit）      ↑ Tail（最新发射）
```

### 精确异常处理
1. 检测到异常 → flush ROB 中异常指令之后的所有指令
2. 异常指令在 ROB 头部 commit 时处理异常（跳转到 handler）
3. 恢复时从异常指令之后开始

### 分支恢复
- 发现预测错误 → 清空 ROB 中该分支之后的指令 → 从正确路径重新取指

---

## 4. 指令调度（Instruction Scheduling）

### Tomasulo 算法
- 动态乱序调度的基础算法，IBM 360/91 首次使用

### 保留站（Reservation Station）
- 每个执行单元有自己的保留站
- 指令等待操作数就绪后再执行
- 操作数通过**公共数据总线（CDB）**广播，所有保留站监听

### Tomasulo 算法三步骤
1. **发射（Issue）**：指令放入保留站，同时进行寄存器重命名
2. **执行（Execute）**：所有操作数就绪时调度到执行单元
3. **写结果（Write Result）**：结果通过 CDB 广播

### 现代超标量处理器关键数字
- 发射宽度：通常 4-6 条/周期
- ROB 大小：100-300+ 项
- 保留站：20-100+ 项

---

## 关键术语
- **ROB**：重排序缓冲，保证顺序提交
- **精确异常**：异常时能精确还原状态
- **Tomasulo 算法**：动态指令调度算法
- **CDB（Common Data Bus）**：公共数据总线，广播执行结果
- **保留站（Reservation Station）**：等待操作数的指令缓冲
- **Predication**：谓词执行，消除难预测分支
- **2位饱和计数器**：稳定的动态分支预测器
