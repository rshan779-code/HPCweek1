# CS6290 Week 4 - 虚拟内存 + 内存技术 + 存储系统 + 容错

---

## 1. 虚拟内存（Virtual Memory）

### 为什么需要虚拟内存
1. **隔离**：多个进程互不干扰，各自看到 0~VMax 的完整地址空间
2. **超量分配**：程序可以使用比物理内存更多的地址空间（不常用的页放磁盘）
3. **简化编程**：无需考虑物理内存布局和多进程共存

### 页（Page）和页帧（Frame）
- 虚拟地址空间和物理内存都按固定大小（通常 4KB）分页
- **页表（Page Table）**：记录虚拟页 → 物理帧的映射

### 页表项（PTE）
| 字段 | 含义 |
|------|------|
| **物理帧号（PFN）** | 对应的物理内存帧 |
| **Valid 位** | 页是否在物理内存中 |
| **Dirty 位** | 页是否被修改（需写回磁盘） |
| **Reference 位** | 是否被访问过（用于置换算法） |
| **权限位** | 读/写/执行权限 |

### 缺页异常（Page Fault）
- 访问 Valid=0 的页 → 触发异常 → OS 从磁盘加载页到内存 → 更新页表 → 重试指令

### TLB（Translation Lookaside Buffer）
- Cache 最近的地址翻译，避免每次都查页表
- TLB 命中：1-2 周期；TLB 缺失：需走页表（数十周期）
- 上下文切换时需刷新 TLB（或用 ASID 区分进程）

### 多级页表
- 32 位地址空间单级页表需 4MB（太大）
- 两级/三级页表：按需分配，节省内存

### VIPT Cache（虚索引物理标签）
- L1 Cache 用虚拟地址索引（并行 TLB 翻译）
- 用物理标签比较（TLB 翻译完成后）
- 前提：index 位不超过页内偏移位（12 位）

---

## 2. 内存技术（Memory）

### SRAM vs DRAM
| 特性 | SRAM | DRAM |
|------|------|------|
| 每 bit 晶体管数 | 6 个 | 1 个 |
| 速度 | 快（纳秒级） | 慢（数十纳秒） |
| 成本 | 高 | 低 |
| 容量（同面积） | 小 | 大 |
| 需要刷新 | 否 | 是（否则数据丢失） |
| 用途 | Cache | 主存 |

### DRAM 工作原理
- 电容存储电荷代表 0/1，一个晶体管控制访问
- 需要定期刷新（读出再写回），因为电容会漏电

### DRAM 访问延迟
```
发送行地址(RAS) → 激活行(~15ns) → 发送列地址(CAS) → 读数据(~15ns)
总延迟：~50-100ns（约 100-200 CPU 周期）
```

### 提高内存带宽的技术
- **突发传输（Burst Mode）**：一次读取整个 cache line（64 字节）
- **双通道内存（Dual Channel）**：两个内存通道并行
- **DDR（Double Data Rate）**：上升沿和下降沿都传数据，带宽翻倍
- **Bank 交错（Bank Interleaving）**：多个 bank 轮流访问，隐藏刷新延迟

---

## 3. 存储系统（Storage）

### 存储的作用
- 持久化存储文件、程序、操作系统
- 作为虚拟内存的 Swap 区（扩展物理内存）

### 磁盘（HDD）访问时间
```
访问时间 = 寻道时间 + 旋转延迟 + 传输时间
```
- **寻道时间（Seek）**：磁头移动到目标磁道，~5-10ms
- **旋转延迟（Rotational Latency）**：等待目标扇区转到磁头下，~4-5ms（7200 RPM）
- 总计：~10ms（比 DRAM 慢 10 万倍）

### SSD（固态硬盘）
- 基于 NAND Flash，无机械延迟（~0.1ms）
- 缺点：写入次数有限，擦除以块为单位（写时复制）

### RAID（磁盘冗余阵列）
| RAID 级别 | 描述 | 特点 |
|------|------|------|
| RAID 0 | 条带化，无冗余 | 性能最好，无容错 |
| RAID 1 | 镜像 | 容错，容量减半 |
| RAID 4 | 条带 + 奇偶校验盘 | 容错，校验盘瓶颈 |
| RAID 5 | 条带 + 分布式奇偶校验 | 容错，无单点瓶颈 |
| RAID 6 | 双重奇偶校验 | 可容两盘同时故障 |

---

## 4. 容错（Fault Tolerance）

### 核心概念
- **可靠性（Reliability）**：系统按规格工作的能力
- **可用性（Availability）**：系统处于正常工作状态的时间比例

### 关键指标
- **MTTF（Mean Time To Failure）**：平均失效前时间
- **MTTR（Mean Time To Repair）**：平均修复时间
- **MTBF = MTTF + MTTR**
- **可用性 = MTTF / MTBF**

### 故障链
```
故障（Fault）→ 错误（Error）→ 失效（Failure）
```

### N 个组件的系统可靠性
- **全部需要工作（串联）**：`MTTF_system = MTTF_component / N`
- **任一工作即可（并联）**：MTTF 大幅提高

### 提高可靠性的方法
| 方法 | 描述 |
|------|------|
| **冗余（Redundancy）** | 多个组件，一个失效还有备份 |
| **ECC（Error Correcting Code）** | 内存错误检测和纠正 |
| **RAID** | 磁盘冗余 |
| **检查点（Checkpointing）** | 定期保存状态，失效后恢复 |
| **热备份（Hot Spare）** | 预备备件，失效时自动切换 |

### Hamming Code / SECDED
- **Hamming Code**：最常用 ECC，可纠正 1 位错误，检测 2 位错误
- **SECDED**：Single Error Correct, Double Error Detect

---

## 关键术语
- **TLB**：地址翻译缓存，加速虚拟→物理地址翻译
- **Page Fault**：缺页异常，触发 OS 从磁盘加载页
- **DRAM**：动态随机存储器，需要定期刷新
- **DDR**：双倍数据速率，上下沿都传数据
- **RAID**：磁盘冗余阵列，提高存储可靠性
- **MTTF/MTTR**：可靠性核心指标
- **ECC**：错误纠正码，内存错误保护
- **SECDED**：单错纠正双错检测
