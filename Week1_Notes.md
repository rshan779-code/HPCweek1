# CS6290 Week 1 - 课程简介 + 性能指标 + 流水线 + ILP

---

## 1. 课程简介（Introduction）

### 核心概念
- **计算机架构**：研究如何利用现有技术设计更快、更节能的计算机
  - 不是设计更快的晶体管（那是半导体工程）
  - 是如何用晶体管构建更好的计算机

### 重要知识点
- **摩尔定律（Moore's Law）**：
  - 每 18~24 个月，同面积芯片上晶体管数量翻倍
  - 架构师目标：将晶体管翻倍转化为：
    - 处理器速度每 18~24 个月翻倍
    - 每次操作能耗减半
    - 内存容量翻倍
- **技术趋势**：必须**预见未来技术**而非设计当前技术，否则产品上市时已过时

---

## 2. 性能指标与评估（Metrics and Evaluation）

### 核心概念
- **延迟（Latency）**：从开始到完成的时间
- **吞吐量（Throughput）**：每秒完成的任务数
- 注意：吞吐量 ≠ 1/延迟（流水线下两者可独立优化）

### 性能公式
```
执行时间 = 指令数 × CPI × 时钟周期时间
性能 = 1 / 执行时间
```
- **指令数**：由程序和 ISA 决定
- **CPI（Cycles Per Instruction）**：由微架构决定
- **时钟周期时间**：由工艺和电路设计决定

### 加速比
- `Speedup = 旧执行时间 / 新执行时间`
- **Amdahl 定律**：加速比受限于不能并行的串行部分
  - `Speedup ≤ 1 / (串行比例 + 并行比例/N)`

### 基准测试（Benchmarks）
- 用真实程序评估处理器性能（如 SPEC CPU）
- 好的 benchmark：代表真实工作负载，不能被"专门优化"
- MIPS/MFLOPS 不可靠（忽略指令复杂度）

---

## 3. 流水线（Pipelining）

### 核心概念
- 将指令执行拆分为多个阶段，多条指令重叠执行
- 类比：石油管道 vs 用桶运油 — 延迟相同但吞吐量大幅提升

### 5 级经典流水线
| 阶段 | 名称 | 功能 |
|------|------|------|
| IF | Instruction Fetch | 取指 |
| ID/RD | Decode / Register Read | 译码/读寄存器 |
| EX | Execute | ALU 运算 |
| MEM | Memory Access | 访存 |
| WB | Write Back | 写回寄存器 |

- **理想 CPI = 1**（每周期完成一条指令）
- **实际 CPI > 1**：因为流水线冒险（Hazards）

### 三种冒险（Hazards）
| 类型 | 描述 | 解决方法 |
|------|------|------|
| **结构冒险（Structural）** | 多条指令争用同一硬件资源 | 增加硬件资源、流水线化资源 |
| **数据冒险（Data）** | 后续指令依赖前指令结果 | 数据转发（Forwarding）、阻塞（Stall） |
| **控制冒险（Control）** | 分支指令导致不确定下一条指令 | 分支预测、延迟分支 |

### 数据转发（Forwarding / Bypassing）
- 不等写回，直接将 EX 或 MEM 阶段结果转发给后续指令
- 减少 stall 周期数

### Load-Use 冒险
- Load 后立即使用加载值 → 必须 stall 1 周期（即使有 forwarding）

---

## 4. 指令级并行（ILP）

### 核心概念
- **ILP（Instruction Level Parallelism）**：程序中可并行执行指令的最大数量
- 目标：CPI < 1（超标量，每周期执行多条指令）

### 依赖类型
| 类型 | 描述 | 是否真依赖 |
|------|------|------|
| **RAW（Read After Write）** | 真依赖，后指令读前指令写的值 | ✅ 真 |
| **WAW（Write After Write）** | 输出依赖，两指令写同一寄存器 | ❌ 假（寄存器重命名可消除） |
| **WAR（Write After Read）** | 反依赖，后指令写前指令读的寄存器 | ❌ 假（寄存器重命名可消除） |

### 寄存器重命名（Register Renaming）
- 将 ISA 寄存器映射到更多物理寄存器
- 消除 WAW 和 WAR 假依赖
- 现代处理器有 100+ 个物理寄存器

### 乱序执行（Out-of-Order Execution）
- 指令不按程序顺序执行，但结果要与顺序执行一致
- 当操作数就绪时立即执行，不等前面未就绪的指令

### 超标量（Superscalar）
- 每周期可发射/执行多条指令
- 现代处理器通常 4-6 条/周期

---

## 关键术语
- **CPI**：每指令周期数（Cycles Per Instruction）
- **IPC**：每周期指令数（Instructions Per Cycle）= 1/CPI
- **ILP**：指令级并行度
- **Forwarding**：数据转发，减少流水线 stall
- **RAW/WAW/WAR**：三种数据依赖类型
- **Amdahl 定律**：加速比上限由串行部分决定
